name: build go linked to pico
on:
  push:
    branches: [main]
  pull_request:

env:
  DEBVER: placeholder

jobs:

  build-it:
    name: Build binary

    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Req dependencies
      run: |
          sudo cp pico/deb.list /etc/apt/sources.list.d/
          sudo apt-get update
          sudo apt-get source libttspico-utils

    - name: see files
      run: ls -l
      #    - name: Tarball
      #      run: tar -zcvf test-files.tgz pico/
      #    - name: Store files
      #      uses: actions/upload-artifact@v3
      #      with:
      #        name: test-files
      #        path: test-files.tgz
      #        retention-days: 1

